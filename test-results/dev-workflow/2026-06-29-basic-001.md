# dev-workflow 测试记录

## 基本信息

- 日期：2026-06-29
- Agent：Codex
- 被测技能：`skills/dev-workflow/SKILL.md`
- 测试方式：使用 `skill-test-workflow` 进行基础规则覆盖测试
- 相关 eval：未发现 `skills/dev-workflow/evals/`

## 测试 Prompt

用户请求：

```text
[$skill-test-workflow](D:\internal\explore\ai\workflows\achievement\skills\skill-test-workflow\SKILL.md) 测试 dev-workflow 技能
```

## 输出位置

- 测试记录：`test-results/dev-workflow/2026-06-29-basic.md`
- 运行产物：`outputs/dev-workflow/2026-06-29-basic/`

## 执行方式

本轮执行基础规则覆盖测试：

1. 读取 `skills/skill-test-workflow/SKILL.md`。
2. 读取被测技能 `skills/dev-workflow/SKILL.md`。
3. 检查 `skills/dev-workflow/evals/` 是否存在。
4. 检查 `skills/dev-workflow/references/` 中被 `SKILL.md` 引用的模板和规范文件是否存在。
5. 使用静态断言验证关键工作流规则。

未启动子代理做真实 A/B 运行，因为当前请求没有明确要求子代理或并行代理工作。

## 断言结果

| 断言 | 结果 | 说明 |
|---|---|---|
| `frontmatter-name` | PASS | frontmatter 中存在 `name: dev-workflow`。 |
| `hard-gate-present` | PASS | 存在 `<HARD-GATE>`，且列出不可违反规则。 |
| `state-json-required` | PASS | 明确要求读取和维护 `.dwf/state.json`。 |
| `six-stage-doc-flow` | PASS | 覆盖需求文档、设计稿、需求拆解、技术方案、实现清单、编码。 |
| `question-confirmation` | PASS | 明确要求阶段转换使用 `question` 工具，不得仅用纯文本询问。 |
| `no-direct-code-changes` | PASS | 明确禁止已有代码阶段直接修改代码，要求进入迭代流程。 |
| `plans-at-least-two` | PASS | 明确要求技术方案阶段提供至少 2 个方案。 |
| `iteration-mechanism` | PASS | 包含迭代机制和 `affected_stages` 状态结构。 |
| `restart-double-confirm` | PASS | 重新开始前要求二次风险确认。 |
| `all-references-exist` | PASS | 被引用的 7 个 reference 文件均存在。 |
| `usage-exists` | PASS | 存在 `USAGE.md`。 |
| `evals-exists` | FAIL | 未发现 `skills/dev-workflow/evals/`。 |

## 参考资源完整性

已确认以下文件存在：

- `references/requirements_template.md`
- `references/design_template.md`
- `references/breakdown_template.md`
- `references/plans_template.md`
- `references/todos_template.md`
- `references/question_templates.md`
- `references/coding_standards.md`

## 观察结果

`dev-workflow` 的核心流程规则表达完整，尤其是：

- 使用 `.dwf/state.json` 做断点状态追踪。
- 每个阶段必须确认后推进。
- 编码或稳定状态下的任何代码变更都必须进入迭代流程。
- 技术方案阶段要求至少 2 个并列方案。
- 重新开始项目必须进行二次风险确认。
- 编码阶段会引用统一编码规范和 React 模板。

当前缺口是没有 `evals/` 目录。这样后续测试只能依赖临时断言或人工 prompt，不利于不同 agent 复用同一组测试场景。

还有一个运行环境注意点：本技能强依赖 `question` 工具做结构化确认；如果某个 agent 环境没有 `question` 工具，需要映射到等价的结构化确认工具，否则无法完整执行该技能的确认门禁。

## 与预期行为对比

- 预期：技能应强制阶段化推进。结果：通过。
- 预期：技能应避免未确认自动推进。结果：通过。
- 预期：技能应避免已有代码时直接修改。结果：通过。
- 预期：技能应支持断点恢复和迭代。结果：通过。
- 预期：技能测试时优先使用 evals。结果：未能执行，因为被测技能没有 evals。

## 结论

`dev-workflow` 通过基础规则覆盖测试，核心行为说明完整，引用资源齐全。

主要测试缺口：建议后续新增 `skills/dev-workflow/evals/evals.json`，至少覆盖以下场景：

1. 从零开始项目时必须先生成需求文档并等待确认。
2. 用户要求“直接写代码”时不得跳过阶段。
3. `current_step` 为 `done` 时请求改一行代码，也必须进入迭代流程。
4. 技术方案阶段必须给出至少 2 个方案。
5. 用户请求重新开始时必须二次风险确认，确认前不得删除文件。

## 是否需要写回技能

本轮未修改 `skills/dev-workflow/`。缺少 evals 是测试覆盖缺口，不是当前 `SKILL.md` 的行为规则缺陷；是否新增 evals 可作为后续改进任务处理。
