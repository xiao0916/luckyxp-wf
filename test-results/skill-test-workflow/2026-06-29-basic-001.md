# skill-test-workflow 测试记录

## 基本信息

- 日期：2026-06-29
- Agent：Codex
- 被测技能：`skills/skill-test-workflow/SKILL.md`
- 测试目的：验证该技能是否能规范“测试其他技能”时的读取顺序、输出位置、A/B 目录结构和写回规则。
- 相关 eval：`skills/skill-test-workflow/evals/evals.json`

## 测试 Prompt

本轮使用该技能自带的 3 个 eval prompt：

1. 测试 `animation-flow` 技能，并记录结果。
2. 对比使用 `dev-workflow` 和不使用 `dev-workflow` 的表现。
3. 跑某个技能自带的 evals，并在发现问题时判断是否更新技能。

## 输出位置

- 测试记录：`test-results/skill-test-workflow/2026-06-29-basic.md`
- 运行产物：`outputs/skill-test-workflow/2026-06-29-basic/`

## 执行方式

本轮执行的是规则覆盖测试，而不是真实子代理 A/B 运行。原因是当前请求没有明确要求使用子代理；可用的子代理工具说明也要求只有在用户明确要求“子代理/并行代理工作”时才派生代理。

测试通过静态断言检查 `SKILL.md` 和 `evals/evals.json` 是否覆盖 eval 中要求的关键行为。

## 断言结果

| 断言 | 结果 | 说明 |
|---|---|---|
| `frontmatter-name` | PASS | frontmatter 中存在 `name: skill-test-workflow`。 |
| `test-results-rule` | PASS | 明确要求测试记录写入 `test-results/<技能名>/` 或 `test-results/<被测技能名>/`。 |
| `outputs-rule` | PASS | 明确要求生成文件、截图、导出物和临时 demo 写入 `outputs/<技能名>/` 或 `outputs/<被测技能名>/`。 |
| `read-tested-skill` | PASS | 明确要求读取 `skills/<被测技能名>/SKILL.md`。 |
| `prefer-evals` | PASS | 明确要求存在 `evals/` 时优先使用。 |
| `ab-without-skill` | PASS | A/B 对比结构包含 `without-skill`。 |
| `writeback-rule` | PASS | 明确限制只有满足特定条件才修改被测技能。 |
| `eval-count-3` | PASS | `evals/evals.json` 包含 3 个 eval prompt。 |

## 与预期行为对比

- Eval 1 预期：测试 `animation-flow` 时先读技能，并将记录和产物分别写入 `test-results/animation-flow/` 与 `outputs/animation-flow/`。当前技能已覆盖。
- Eval 2 预期：A/B 对比时建立 `with-skill/` 和 `without-skill/` 分组。当前技能已覆盖。
- Eval 3 预期：优先读取被测技能自带 `evals/`，并且只在能改进通用行为或用户明确要求时写回技能。当前技能已覆盖。

## 观察结果

第一次断言脚本使用了 PowerShell 数组嵌套，导致输出被展开，结果不可读。修正为 `[pscustomobject]` 后，断言结果稳定，全 8 项通过。

本轮检查确认 `skills/skill-test-workflow/` 下只有技能源码和 eval 文件，没有新增测试产物。

## 结论

`skill-test-workflow` 通过本轮基础规则覆盖测试。它已经能表达仓库中“测试技能”部分的关键约束，尤其是测试记录与运行产物的分离。

## 是否需要写回技能

暂不需要。当前发现的问题属于测试脚本写法问题，不是技能通用行为缺陷。
