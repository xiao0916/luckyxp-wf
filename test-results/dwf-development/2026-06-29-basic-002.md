# dwf-development 测试记录

## 基本信息

- 日期：2026-06-29
- Agent：Codex
- 被测技能：`skills/dwf-development/SKILL.md`
- 测试目的：使用 `skill-test-workflow` 验证 `dwf-development` 的三个 eval 场景、输出路径、确认门禁和静态约束。
- 相关 eval：`skills/dwf-development/evals/evals.json`

## 测试 Prompt

本轮参考被测技能自带的三个 eval：

1. `帮我生成技术方案`
2. `需求拆解一下这个功能`
3. `创建实现清单`

## 输出位置

- 测试记录：`test-results/dwf-development/2026-06-29-basic-002.md`
- 运行产物：`outputs/dwf-development/2026-06-29-basic-002/`
- 最小真实样例：
  - `outputs/dwf-development/2026-06-29-basic-002/eval-1-plans/generated/04-技术方案/技术方案.md`
  - `outputs/dwf-development/2026-06-29-basic-002/eval-2-breakdown/generated/03-需求拆解文档/需求拆解文档.md`
  - `outputs/dwf-development/2026-06-29-basic-002/eval-3-todos/generated/05-实现清单/实现清单.md`
- 静态检查结果：见下文。

## 运行方式

本轮在 `outputs/dwf-development/2026-06-29-basic-002/` 下创建三个隔离样例目录。每个样例中的 `fixture-input/` 表示测试前已经存在的输入文档，不视为 `dwf-development` 主动创建的工作流目录；`generated/` 表示按被测技能规则生成的目标阶段文档。

由于 `dwf-development` 要求每个阶段生成后请求用户确认并停止，本轮没有自动串联推进完整 dev-workflow，也没有创建 `.dwf/state.json`。

## 观察结果

### Eval 1：生成技术方案

- 产物路径：`eval-1-plans/generated/04-技术方案/技术方案.md`
- 结果：通过。
- 观察：文档包含 2 个并列方案、方案对比表、推荐方案、第三方库、接口需求、数据模型、受影响文件、风险与缓解策略。
- 停点：生成技术方案后应等待用户选择方案。

### Eval 2：需求拆解

- 产物路径：`eval-2-breakdown/generated/03-需求拆解文档/需求拆解文档.md`
- 结果：通过。
- 观察：文档包含目标端、页面与功能拆解、跨页面共享模块、素材/依赖清单和范围边界。
- 停点：生成需求拆解后应等待用户确认。

### Eval 3：创建实现清单

- 产物路径：`eval-3-todos/generated/05-实现清单/实现清单.md`
- 结果：通过。
- 观察：任务按依赖顺序拆分，每个任务包含优先级、任务内容、编码规范检查和验证标准。
- 停点：生成实现清单后应等待用户确认。

## 静态检查结果

| 检查项 | 结果 | 说明 |
|---|---|---|
| frontmatter | 通过 | 存在 `name` 和 `description`。 |
| 只处理三个阶段 | 通过 | HARD-GATE 明确只创建或更新 03、04、05 三类文档。 |
| 不创建完整工作流 | 通过 | 明确不要主动创建 01、02、06、07。 |
| 覆盖前确认 | 通过 | 明确已有目标文件时必须先询问。 |
| 中文输出 | 通过 | 明确全程使用中文。 |
| 运行模式检测 | 通过 | 明确检查 `.dwf/state.json` 并区分独立模式和工作流模式。 |
| eval 覆盖 | 通过 | eval 覆盖技术方案、需求拆解、实现清单三个入口。 |
| 模板引用 | 需关注 | `SKILL.md` 引用了 `references/breakdown_template.md`、`references/plans_template.md`、`references/todos_template.md`，但当前 `skills/dwf-development/` 下未发现 `references/` 目录。 |

## 与预期行为对比

- Eval 1 预期保存到 `04-技术方案/技术方案.md` 且至少 2 个方案；本轮样例符合。
- Eval 2 预期保存到 `03-需求拆解文档/需求拆解文档.md`；本轮样例符合。
- Eval 3 预期保存到 `05-实现清单/实现清单.md`；本轮样例符合。
- `skill-test-workflow` 预期测试记录进入 `test-results/<技能名>/`、运行产物进入 `outputs/<技能名>/`；本轮符合。

## 结论

`dwf-development` 的核心行为描述、阶段边界和 eval 场景整体可用。本轮最小真实样例验证了三个目标文档都能按预期结构生成，并且没有把运行产物写入 `skills/` 源码目录。

需要关注的问题是：技能正文要求遵循 `references/` 模板，但仓库中当前没有这些模板文件。这会导致未来 agent 无法严格按引用模板执行，只能根据正文描述推断结构。

## 是否需要写回技能

建议后续补充 `skills/dwf-development/references/` 下的三个模板文件，或将技能正文改为“不存在模板文件时按内置字段清单生成”。本轮仅测试并记录观察，未修改 `dwf-development` 技能源码。
